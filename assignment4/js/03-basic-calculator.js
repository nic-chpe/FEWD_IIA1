let total = 0;
// ADD A PRIVATE FUNCTION CALLED CALCULATE
const calculate = (x, y, method) => {
    switch (method) {
        case 'add':
            return x + y;
            break;
        case 'substract':
            return x - y;
            break;
        case 'multiply':        
    }
}